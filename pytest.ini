[pytest]
testpaths = tests
markers =
    e2e: live integration tests against a real EduPage server (require EDUPAGE_E2E=1 + EDUPAGE_USERNAME/PASSWORD/SUBDOMAINS). Local-only: results contain real student data and must never run in public CI.
addopts = -ra